# Given a string containing someone's first and last name, return their initials

# Split names into list
# Use indexing to get the first char of each item in list
# Use .upper() capitalise
# Return initials

def initials(name):
    names = name.split(" ")
    return f"{list[0][0].upper()}.{list[0][0].upper()}"